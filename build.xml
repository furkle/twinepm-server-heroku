<?xml version="1.0" encoding="UTF-8" ?>
<project name="twinepm-server-heroku" default="run-stable-vm">
    <!-- START VIRTUAL MACHINE TASKS -->

    <!-- Runs from host machine. -->
    <target name="destroy-vm">
        <exec
            passthru="true"
            command="vagrant destroy">
        </exec>
    </target>

    <!-- Runs from host machine. -->
    <target name="run-stable-vm">
        <exec
            passthru="true"
            command="
            TWINEPM_BRANCH=master &amp;&amp;
            export TWINEPM_BRANCH &amp;&amp;
            vagrant up">
        </exec>
    </target>

    <!-- Runs from host machine. -->
    <target name="run-dev-vm">
        <exec
            passthru="true"
            command="
            TWINEPM_BRANCH=dev &amp;&amp;
            export TWINEPM_BRANCH &amp;&amp;
            vagrant up">
        </exec>
    </target>

    <!-- Runs from host machine/VM. -->
    <target name="get-vm-dependencies">
        <exec
            passthru="true"
            command="
            curl -fsSL https://download.docker.com/linux/ubuntu/gpg | apt-key add - &amp;&amp;
            apt-key fingerprint 0EBFCD88 &amp;&amp;
            add-apt-repository
                &quot;deb [arch=amd64]
                https://download.docker.com/linux/ubuntu
                $(lsb_release -cs)
                stable&quot; &amp;&amp;
            apt-get update &amp;&amp;
            apt-get install -y docker-ce">
        </exec>
    </target>

    <!-- Runs from host machine. -->
    <target name="ssh">
        <exec
            passthru="true"
            command="vagrant ssh">
        </exec>
    </target>

    <!-- END VIRTUAL MACHINE TASKS -->

    <!-- START CONTAINER TASKS -->

    <!-- Runs inside host machine or VM. -->
    <target name="build-container-images">
        <exec
            passthru="true"
            command="
            BUILD_ARGS='
                --build-arg TWINEPM_BRANCH=$TWINEPM_BRANCH
                --build-arg TWINEPM_BRANCH=$TWINEPM_BRANCH
                --build-arg TWINEPM_REPO_SITE=$TWINEPM_REPO_SITE
                --build-arg TWINEPM_REPO_OWNER=$TWINEPM_REPO_OWNER
                --build-arg TWINEPM_REPO_NAME=$TWINEPM_REPO_NAME' &amp;&amp;
            docker build $BUILD_ARGS -t twinepm_logic /etc/twinepm-server-heroku/src &amp;&amp;
            docker build $BUILD_ARGS -t twinepm_postgresql /etc/twinepm-server-heroku/postgresql &amp;&amp;
            docker build $BUILD_ARGS -t twinepm_redis /etc/twinepm-server-heroku/redis">
        </exec>
    </target>

    <target name="remove-container-images">
        <exec
            passthru="true"
            command="
            docker rmi twinepm_logic &amp;&amp;
            docker rmi twinepm_postgresql &amp;&amp;
            docker rmi twinepm_redis">
        </exec>
    </target>

    <target name="rebuild-container-images">
        <exec
            passthru="true"
            command="
            phing remove-container-images &amp;&amp;
            phing build-container-images">
        </exec>
    </target>

    <!-- Runs inside host machine or VM. -->
    <target name="run-containers">
        <exec
            passthru="true"
            command="
            docker run --name twinepm_logic -d -p 443:80 twinepm_logic &amp;&amp;
            docker run --name twinepm_postgresql -d twinepm_postgresql &amp;&amp;
            docker run --name twinepm_redis -d twinepm_redis redis-server --appendonly yes">
        </exec>
    </target>

    <target name="start-containers">
        <exec
            passthru="true"
            command="
            docker start twinepm_logic &amp;&amp;
            docker start twinepm_postgresql &amp;&amp;
            docker start twinepm_redis">
        </exec>
    </target>

    <target name="stop-containers">
        <exec
            passthru="true"
            command="
            docker stop twinepm_logic &amp;&amp;
            docker stop twinepm_postgresql &amp;&amp;
            docker stop twinepm_redis">
        </exec>
    </target>

    <target name="stop-containers">
        <exec
            passthru="true"
            command="
            docker stop twinepm_logic &amp;&amp;
            docker stop twinepm_postgresql &amp;&amp;
            docker stop twinepm_redis">
        </exec>
    </target>

    <target name="remove-containers">
        <exec
            passthru="true"
            command="
            docker rm twinepm_logic &amp;&amp;
            docker rm twinepm_postgresql &amp;&amp;
            docker rm twinepm_redis">
        </exec>
    </target>

    <target name="restart-containers">
        <exec
            passthru="true"
            command="
            phing stop-containers &amp;&amp;
            phing remove-containers &amp;&amp;
            phing rebuild-container-images &amp;&amp;
            phing run-containers">
        </exec>
    </target>

    <!-- Runs inside webserver/logic container. -->
    <target name="get-logic-dependencies">
        <exec
            passthru="true"
            command="
            apt-get install -y
                software-properties-common
                gnupg
                locales &amp;&amp;
            LANG=en_US.UTF-8 &amp;&amp;
            LANGUAGE=en_US:en &amp;&amp;
            LC_ALL=en_US.UTF-8 &amp;&amp;
            localedef -i en_US -f UTF-8 en_US.UTF-8 &amp;&amp;
            /usr/sbin/update-locale LANG=$LANG &amp;&amp;
            apt-key adv --keyserver keyserver.ubuntu.com --recv-keys 4F4EA0AAE5267A6C &amp;&amp;
            add-apt-repository -y ppa:ondrej/php &amp;&amp;
            apt-get update &amp;&amp;
            apt-get install -y
                composer
                php7.1
                php7.1-fpm
                php7.1-mbstring
                php7.1-xml
                php7.1-zip
                unzip
                zip">
        </exec>
    </target>

    <!-- Runs inside webserver/logic container. -->
    <target name="setup-nginx">
        <exec
            passthru="true"
            command="
                printf &quot;
                    user nginx;\n
                    worker_processes 1;\n
                    \n
                    error_log /var/log/nginx/error.log warn;\n
                    pid /var/run/nginx.pid;\n
                    \n
                    events {\n
                        worker_connections 1024;\n
                    }\n
                    \n
                    http {\n
                        include /etc/nginx/mime.types;\n
                        default_type application/octet-stream;\n
                        \n
                        log_format main '\$remote_addr - \$remote_user [\$time_local] \&quot;\$request\&quot;'\n
                                        '\$status \$body_bytes_sent \&quot;\$http_referer\&quot;'\n
                                        '\&quot;\$http_user_agent\&quot; \&quot;\$http_x_forwarded_for\&quot;';\n
                        \n
                        access_log /var/log/nginx/access.log main;\n
                        \n
                        sendfile on;\n
                        \n
                        keepalive_timeout 65;\n
                        \n
                        gzip on;\n
                        \n
                        include /etc/nginx/conf.d/*.conf;\n
                        \n
                        server {\n
                            root /usr/share/nginx/html/;\n
                            \n
                            location / {\n
                                index index.php index.html index.htm;\n
                            }\n
                            \n
                            location ~ [^/]\.php(\\/|\$) {\n
                                fastcgi_split_path_info ^(.+?\.php)(\\/.*)?$;\n
                                try_files \$fastcgi_script_name =404;\n
                                \n
                                # Mitigate https://httpoxy.org/ vulnerabilities\n
                                fastcgi_param HTTP_PROXY \&quot;\&quot;;\n
                                \n
                                fastcgi_pass unix:/run/php/php7.1-fpm.sock;\n
                                fastcgi_index index.php;\n
                                include fastcgi_params;\n
                            }\n
                            \n
                            location ~ /\.ht {\n
                                deny all;\n
                            }\n
                        }\n
                    }\n
                &quot; &gt; /etc/nginx/nginx.conf &amp;&amp;

                echo (head -n -1 /etc/nginx/mime.types; printf &quot;    text/php php php7;\n}&quot;) &amp;&amp;

                cat /etc/nginx/mime.types &amp;&amp;

                echo &quot;&lt;?php require_once '/etc/twinepm-server-heroku/index.php';&quot; &gt;
                    /usr/share/nginx/html/index.php &amp;&amp;

                echo 'Finished nginx setup.'">
        </exec>
    </target>

    <!-- Runs inside webserver/logic container. -->
    <target name="install-php-packages">
        <exec
            passthru="true"
            command="
            cd /etc/twinepm-server-heroku &amp;&amp;
            composer install --no-plugins --no-scripts">
        </exec>
    </target>

    <!-- END CONTAINER TASKS -->
</project>